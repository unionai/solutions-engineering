## Running the examples in this project
1. Navigate into the directory 
2. Set `admin.endpoint`, `task.org` and `task.project` in `.flyte/config.yaml`
3. Run `uv sync`
4. Run `uv run python main.py` or `uv run flyte run main.py <task_name>`