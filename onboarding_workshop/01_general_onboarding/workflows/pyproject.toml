[project]
name = "flyte-workshop-s1"
version = "0.1.0"
description = "Flyte Workshop S1: one example workflow per chapter"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flyte>=2.0.11",
    "emoji>=2.0",
]
