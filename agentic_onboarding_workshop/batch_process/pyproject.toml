[project]
name = "batch-process"
version = "0.1.0"
description = "Exercise 1: Batch Document Processing Pipeline"
requires-python = ">=3.12"
dependencies = [
    "flyte>=2.0.0b35",
    "anthropic",
    "chromadb",
    "unionai-reuse>=0.1.9",
]

[tool.hatch.build.targets.wheel]
packages = ["."]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
